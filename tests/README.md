# ush tests

This directory contains shell regression tests for control-flow features.

Run all tests from the repository root with:

```sh
make test
```

Or run a single test script directly:

```sh
./tests/test_if.sh
```